// The extension's entry point. It contributes nothing at run time yet:
// package.json names no `main`, so VS Code never loads it.
export function activate(): void {}

export function deactivate(): void {}
